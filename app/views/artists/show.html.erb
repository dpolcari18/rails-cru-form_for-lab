<p id="notice"><%= notice %></p>

<h1>
  Artist: <%= @artist.name %>
</h1>

<p>
  <strong>Bio:</strong>
  <%= @artist.bio %>
</p>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
